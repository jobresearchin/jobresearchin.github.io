---
pagination:
  data: translations
  size: 1
  alias: lang
permalink: "{% if lang.key == 'ja' %}/services/{% else %}/{{ lang.key }}/services/{% endif %}index.html"
---

<!DOCTYPE html>
<html lang="{{ lang.key }}">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{ lang.servicesDetail.title }} | {{ lang.meta.siteName }}</title>
  <meta name="description" content="{{ lang.servicesDetail.description }}" />

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;500;700&display=swap" rel="stylesheet" media="print" onload="this.media='all'" />
  <link rel="stylesheet" href="/style.css" />
  <link rel="alternate" hreflang="ja" href="https://corp.jobresearchin.com/services/" />
  <link rel="alternate" hreflang="en" href="https://corp.jobresearchin.com/en/services/" />
  <meta property="og:type" content="website" />
  <meta property="og:title" content="{{ lang.servicesDetail.title }} | {{ lang.meta.siteName }}" />
  <meta property="og:description" content="{{ lang.servicesDetail.description }}" />
  <meta property="og:image" content="https://corp.jobresearchin.com/static/og-image.jpg" />
  <meta property="og:url" content="https://corp.jobresearchin.com/{% if lang.key == 'ja' %}services/{% else %}{{ lang.key }}/services/{% endif %}" />
  <meta property="og:site_name" content="{{ lang.meta.siteName }}" />
  <meta name="twitter:card" content="summary_large_image" />

  <script async src="https://www.googletagmanager.com/gtag/js?id=G-X754F6MGCM"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-X754F6MGCM');
  </script>
</head>

<body>
  <div class="mobile-sidebar" id="mobile-sidebar">
    <button class="menu-close-btn" id="menu-close-btn">&times;</button>
    <nav>
      <ul>
        <li><a href="{% if lang.key == 'ja' %}/{% else %}/{{ lang.key }}/{% endif %}#service-overview" class="menu-link">{{ lang.header.navOverview }}</a></li>
        <li><a href="{% if lang.key == 'ja' %}/{% else %}/{{ lang.key }}/{% endif %}#researchers" class="menu-link">{{ lang.header.navResearchers }}</a></li>
        <li><a href="{% if lang.key == 'ja' %}/{% else %}/{{ lang.key }}/{% endif %}#participants" class="menu-link">{{ lang.header.navParticipants }}</a></li>
        <li><a href="{% if lang.key == 'ja' %}/{% else %}/{{ lang.key }}/{% endif %}#team" class="menu-link">{{ lang.header.navTeam }}</a></li>
        <li><a href="{% if lang.key == 'ja' %}/{% else %}/{{ lang.key }}/{% endif %}#company-profile" class="menu-link">{{ lang.header.navCompany }}</a></li>
        <li><a href="{% if lang.key == 'ja' %}/services/{% else %}/{{ lang.key }}/services/{% endif %}" class="menu-link">{{ lang.servicesDetail.title }}</a></li>
        <li><a href="{% if lang.key == 'ja' %}/news/{% else %}/{{ lang.key }}/news/{% endif %}" class="menu-link">{{ lang.news.title }}</a></li>
      </ul>
    </nav>
  </div>
  <div class="sidebar-overlay" id="sidebar-overlay"></div>

  <div class="site-wrapper">
    <header>
      <div class="nav-container">
        <a href="{% if lang.key == 'ja' %}/{% else %}/{{ lang.key }}/{% endif %}" class="brand-logo">
          {% image "static/logo.png", "Researchin Logo", "(max-width: 600px) 150px, 200px" %}
        </a>
        <nav class="desktop-nav">
          <a href="{% if lang.key == 'ja' %}/{% else %}/{{ lang.key }}/{% endif %}#service-overview">{{ lang.header.navOverview }}</a>
          <a href="{% if lang.key == 'ja' %}/{% else %}/{{ lang.key }}/{% endif %}#researchers">{{ lang.header.navResearchers }}</a>
          <a href="{% if lang.key == 'ja' %}/{% else %}/{{ lang.key }}/{% endif %}#participants">{{ lang.header.navParticipants }}</a>
          <a href="{% if lang.key == 'ja' %}/{% else %}/{{ lang.key }}/{% endif %}#team">{{ lang.header.navTeam }}</a>
          <a href="{% if lang.key == 'ja' %}/{% else %}/{{ lang.key }}/{% endif %}#company-profile">{{ lang.header.navCompany }}</a>
          <a href="{% if lang.key == 'ja' %}/services/{% else %}/{{ lang.key }}/services/{% endif %}">{{ lang.servicesDetail.title }}</a>
          <a href="{% if lang.key == 'ja' %}/news/{% else %}/{{ lang.key }}/news/{% endif %}">{{ lang.news.title }}</a>
        </nav>
        <div class="header-right">
          <div class="lang-switcher desktop-lang-switcher">
            <a href="/services/" {% if lang.key=='ja' %}class="active"{% endif %}>JP</a>
            <span>/</span>
            <a href="/en/services/" {% if lang.key=='en' %}class="active"{% endif %}>EN</a>
          </div>
          <a href="{{ lang.header.navContactLink }}" class="btn contact-btn" target="_blank" rel="noopener noreferrer">{{ lang.header.navContact }}</a>
          <button class="hamburger-menu" id="hamburger-menu" aria-label="メニューを開く"><span></span><span></span></button>
        </div>
      </div>
    </header>

    <main>
      <section class="page-header">
        <div class="container">
          <h1 class="page-title">{{ lang.servicesDetail.title }}</h1>
          <p class="page-description">{{ lang.servicesDetail.description }}</p>
        </div>
      </section>

      <section class="services-detail-content">
        <div class="container">
          <!-- メインサービス -->
          <div class="service-section">
            <h2 class="service-section-title">{{ lang.servicesDetail.mainService.title }}</h2>
            <p class="service-section-description">{{ lang.servicesDetail.mainService.description }}</p>
            
            <div class="features-list">
              <h3>主な機能</h3>
              <ul>
                {% for feature in lang.servicesDetail.mainService.features %}
                <li>{{ feature }}</li>
                {% endfor %}
              </ul>
            </div>
          </div>

          <!-- その他のサービス -->
          <div class="service-section">
            <h2 class="service-section-title">{{ lang.servicesDetail.additionalServices.title }}</h2>
            
            <div class="additional-services-grid">
              {% for service in lang.servicesDetail.additionalServices.list %}
              <div class="additional-service-card">
                <h3 class="service-card-title">{{ service.title }}</h3>
                <p class="service-card-description">{{ service.description }}</p>
              </div>
              {% endfor %}
            </div>
          </div>

          <!-- CTAセクション -->
          <div class="services-cta">
            <h2>サービスに関するお問い合わせ</h2>
            <p>詳しい情報やご相談については、お気軽にお問い合わせください。</p>
            <a href="{{ lang.header.navContactLink }}" class="btn btn-primary" target="_blank" rel="noopener noreferrer">{{ lang.header.navContact }}</a>
          </div>
        </div>
      </section>
    </main>

    <footer>
      <div class="container">
        <div class="footer-content">
          <div class="footer-left">
            <div class="footer-logo">
              {% image "static/logo.png", "Researchin Logo", "(max-width: 600px) 120px, 150px" %}
            </div>
          </div>
          <div class="footer-right">
            <p>{{ lang.footer.contactPrefix }} <a href="mailto:{{ lang.footer.contactEmail }}">{{ lang.footer.contactEmail }}</a></p>
          </div>
        </div>
      </div>
    </footer>
  </div>

  <script>
    // モバイルメニューの制御
    const hamburgerMenu = document.getElementById('hamburger-menu');
    const mobileSidebar = document.getElementById('mobile-sidebar');
    const menuCloseBtn = document.getElementById('menu-close-btn');
    const sidebarOverlay = document.getElementById('sidebar-overlay');
    const menuLinks = document.querySelectorAll('.menu-link');

    function openMenu() {
      mobileSidebar.classList.add('open');
      sidebarOverlay.classList.add('open');
      document.body.style.overflow = 'hidden';
    }

    function closeMenu() {
      mobileSidebar.classList.remove('open');
      sidebarOverlay.classList.remove('open');
      document.body.style.overflow = '';
    }

    hamburgerMenu.addEventListener('click', openMenu);
    menuCloseBtn.addEventListener('click', closeMenu);
    sidebarOverlay.addEventListener('click', closeMenu);

    menuLinks.forEach(link => {
      link.addEventListener('click', () => {
        closeMenu();
      });
    });

    // ESCキーでメニューを閉じる
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape') {
        closeMenu();
      }
    });
  </script>
</body>
</html>